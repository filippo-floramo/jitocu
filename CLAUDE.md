# jitocu

Project instructions live in [AGENTS.md](./AGENTS.md) so that every model and
tool reads the same file. It is imported below, so it loads automatically.

@AGENTS.md

Deeper reference — layering, technical choices and their reasoning, the search
abstraction, the custom prompts, the time-entry DSL, known rough edges — is in
[ARCHITECTURE.md](./ARCHITECTURE.md). Read it before non-trivial work.
